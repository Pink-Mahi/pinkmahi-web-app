# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting, Functions, Firestore, and Realtime Database on Merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      # Check out the latest commit
      - uses: actions/checkout@v4

      # Install dependencies
      - run: npm install

      # Build the project (assuming you have a build step)
      - run: npm run build

      # Deploy Firebase Hosting, Functions, Firestore, and Realtime Database
      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PINK_MAHI_282C2 }}
          projectId: pink-mahi-282c2
        run:
          |
          # Deploy Hosting, Functions, Firestore, and Realtime Database
          firebase deploy --only hosting,functions,firestore,database

